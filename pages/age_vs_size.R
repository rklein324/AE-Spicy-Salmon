age_vs_size_page <- tabPanel(
  "Salmon Population Map",
  titlePanel("Salmon Population Map"),
  
  sidebarLayout(
    # creates sidebar panel
    sidebarPanel(
      h3("stuff")
    ),
    
    # creates main panel
    mainPanel(
      h3("stuff")
    )
  )
)