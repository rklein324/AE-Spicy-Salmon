age_vs_time_page <- tabPanel(
  "Size/Age Over Time",
  titlePanel("Look at the Change in Salmon Size or Age Over Time"),
  
  sidebarLayout(
    # creates sidebar panel
    sidebarPanel(
      h3("stuff")
    ),
    
    # creates main panel
    mainPanel(
      h3("stuff")
    )
  )
)