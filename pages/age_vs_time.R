age_vs_time_page <- tabPanel(
  "Age vs. Time",
  titlePanel("Salmon Size & Age vs. Time"),
  
  sidebarLayout(
    # creates sidebar panel
    sidebarPanel(
      h3("stuff")
    ),
    
    # creates main panel
    mainPanel(
      h3("stuff")
    )
  )
)