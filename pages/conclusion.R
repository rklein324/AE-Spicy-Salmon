#-----------------------------------
# Creates summary page
#-----------------------------------
conclusion_page <- tabPanel(
  "Conclusion",
  titlePanel("Conclusion"),
  
  sidebarLayout(
    # creates sidebar panel
    sidebarPanel(
      h3("stuff")
    ),
    
    # creates main panel
    mainPanel(
      h3("stuff")
    )
  )
)