harvesting_page <- tabPanel(
  "Harvesting",
  titlePanel("Harvesting"),
  
  sidebarLayout(
    # creates sidebar panel
    sidebarPanel(
      h3("stuff")
    ),
    
    # creates main panel
    mainPanel(
      h3("stuff")
    )
  )
)